<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.adam_lincoln.android.arrowforgithub"
    >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Color"
        android:name=".presentation.app.AppApplication"
        tools:ignore="GoogleAppIndexingWarning"
        >
        <activity
            android:name=".presentation.splash.SplashActivity"
            android:label="@string/activity_splash_title"
            android:theme="@style/AppTheme.NoColor"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.home.HomeActivity"
            android:label="@string/activity_home_title"
            />
        <activity
            android:name=".presentation.theme.ThemeActivity"
            android:label="@string/dialog_title_theme"
            />
    </application>
</manifest>